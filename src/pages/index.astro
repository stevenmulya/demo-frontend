<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Portrait Scroll</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        html, body {
            width: 100%;
            height: 100vh; /* Changed to 100vh for mobile-friendly height */
            font-family: "Inter";
            background-color: #000;
            color: #fff;
            overflow-x: hidden; /* Prevent horizontal scroll */
        }
        header, footer {
            width: 100%;
            padding: 1em; /* Reduced padding for mobile */
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: fixed;
            z-index: 2;
        }
        header {
            top: 0;
        }
        footer {
            bottom: 0;
        }
        header p, footer p {
            font-size: 12px; /* Reduced font size for mobile */
            font-weight: 400;
            line-height: 1.5;
            opacity: 0.5;
        }
        header p#logo {
            opacity: 1;
        }
        .nav-links {
            display: flex;
            gap: 1em; /* Reduced gap for mobile */
        }
        .nav-links a {
            color: #fff;
            text-decoration: none; /* Remove underline from links */
            font-size: 12px;
        }
        .slider-wrapper {
            position: fixed;
            width: 100%;
            height: 100vh;
            overflow: hidden;
        }
        canvas {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
        }
        .overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100vh;
            background: radial-gradient(circle, rgba(0, 0, 0, 0) 75%, rgba(0, 0, 0, 0.5) 100%);
            z-index: 1;
        }
        /* Responsive adjustments */
        @media (max-width: 600px) {
            .nav-links {
                gap: 0.5em; /* Further reduce gap on smaller screens */
            }
            header p, footer p, .nav-links a{
                font-size: 10px;
            }
        }
    </style>
</head>
<body>
    <header>
        <nav>
            <div class="side-info">
                <p id="logo">Portrait Scroll</p>
                <p>by Steven Mulya Tjendratama</p>
            </div>
            <div class="nav-links">
                <p><a href="/projects">Projects</a></p>
                <p><a href="/about">About</a></p>
            </div>
        </nav>
    </header>

    <main>
        <div class="slider-wrapper">
            <canvas></canvas>
        </div>
        <div class="overlay"></div>
    </main>

    <footer>
        <p>Portofolio #001</p>
        <p>Movie Production</p>
    </footer>

    <script type="module" src="/main.js"></script>
</body>
</html>